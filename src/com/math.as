package com
{
	import flash.utils.Dictionary;

	/*========================================================================================
	PRECOMPILED MATH CONSTANTS
	========================================================================================*/
	public class math
	{
		/** PI shortened to 3.14159 */
		static public const pi:Number = 3.14159;
		
		/** two PI */
		static public const pi_2pi:Number = pi * 2;
		
		/** four divided by pi */
		static public const pi_4divpi:Number = 4 / pi;
		
		/** PI divided by 2 */
		static public const pi_div2:Number = Math.PI / 2;
		
		/** PI divided by 4 */
		static public const pi_div4:Number = Math.PI / 4;
	}
}